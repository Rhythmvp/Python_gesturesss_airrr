# Air Control - Gesture + Voice (Windows)

## Quick setup (Windows)

1. Make virtualenv and activate:
```powershell
python -m venv venv
venv\Scripts\activate

2.
pip install -r requirements.txt

